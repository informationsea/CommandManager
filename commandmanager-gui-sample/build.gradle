apply plugin: 'application'

mainClassName = 'info.informationsea.commandmanager.gui.sample.LineEditor'

dependencies {
    compile project(':commandmanager-gui')
    compile project(':commandmanager-cli-sample')
}